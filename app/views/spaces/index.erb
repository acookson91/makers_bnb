<div class='spacelist'>
  <h1 id='title-index-spaces'>Spaces to stay</h1>


<!-- <ul id='spaces'>
  <% @spaces.each do |space| %>
    <% unless space.available_date == "BOOKED" %>
      <li class='space'>Name: <%= space.name  %><br>
        Desc: <%= space.desc  %> <br>
        Price: <%= space.price  %> <br>
        <form action="/booked" method="post">
          <input type="text" name='space_id' value= <%=space.id %> >
          <input type='submit' value='Book'>
        </form>
      </li>
    <% end %>

  <% end %>
</ul> -->

  <div class="list-group">
    <% @spaces.each do |space| %>
      <% if space.bookings.empty? == false %>
        <% space.bookings.each do |booking| %>
          <% if booking.status == "Pending" %>
            <a href="#" class="list-group-item">
              <span class="list-group-item-heading"><%= space.name %></span>
              <form action="/booked" method="post" class="list-group-button">
                <input type="text" name='space_id' hidden value= <%=space.id %>>
                <input type="submit" value="Request booking" class="btn btn-lg"></button>
              </form>
              <p class="list-group-item-desc"><%= space.desc %></p>
              <p class="list-group-item-price">£<%= space.price %> per night</p>
            </a>
          <% end %>
        <% end %>
      <% else %>
          <a href="#" class="list-group-item">
            <span class="list-group-item-heading"><%= space.name %></span>
            <form action="/booked" method="post" class="list-group-button">
              <input type="text" name='space_id' hidden value= <%=space.id %>>
              <input type="submit" value="Request booking" class="btn btn-lg"></button>
            </form>
            <p class="list-group-item-desc"><%= space.desc %></p>
            <p class="list-group-item-price">£<%= space.price %> per night</p>
          </a>
      <% end %>
    <% end %>

    <form action='/spaces/new' >
      <input type='submit' value='New space' class="btn btn-lg">
    </form>
      <!-- <a href="#" class="list-group-item">
        <h4 class="list-group-item-heading">Second List Group Item Heading</h4>
        <p class="list-group-item-text">List Group Item Text</p>
      </a>
      <a href="#" class="list-group-item">
        <h4 class="list-group-item-heading">Third List Group Item Heading</h4>
        <p class="list-group-item-text">List Group Item Text</p>
      </a> -->
  </div>

</div>
